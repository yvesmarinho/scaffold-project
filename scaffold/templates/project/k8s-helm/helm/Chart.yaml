apiVersion: v2
name: my-app
description: A Helm chart for Kubernetes — generated by Enterprise Scaffold Project Template
type: application

# chart version — bump when chart structure changes (semver)
version: 0.1.0

# appVersion — matches the application image tag
appVersion: "1.0.0"

keywords:
  - app
  - kubernetes
  - helm

maintainers:
  - name: platform-team
    email: platform@example.com
